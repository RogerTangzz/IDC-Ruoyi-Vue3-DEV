# .env.example
# 复制为 .env.development 用于本地开发：
# Linux/Mac: cp .env.example .env.development
# Windows PowerShell: Copy-Item .env.example .env.development

# 应用标题（可用于浏览器标题、登录页等）
VITE_APP_TITLE=IDC运维管理系统

# 运行环境标识（通常用于日志或条件构建）
VITE_APP_ENV=development

# 前端调用后端的统一前缀（推荐通过 Vite 代理到后端）
# 如果你用 RuoYi-Boot 本地后端，常见 target 是 http://localhost:8080
VITE_APP_BASE_API=/dev-api
